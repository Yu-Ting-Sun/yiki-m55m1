# 憶起後端環境變數範本。複製成 .env 再填值（.env 已被 git 忽略）：
#   cp .env.example .env

# --- LLM（遊記、景點介紹、導遊精靈小憶）---------------------------------
# NCKU LiteLLM proxy 發的 key；沒有就把 LLM_MOCK 設 1 用假資料測。
OPENAI_API_KEY=
# LLM_MOCK=1
# LLM_MODEL=gemini-3-flash

# --- Foursquare Places（附近 POI）--------------------------------------
# 新版 Service API Key。沒填就退回免費的 Overpass。
FOURSQUARE_API_KEY=
